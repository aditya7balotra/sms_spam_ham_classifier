# sms_spam_ham_classifier

<br>
so , this is a model which is based on the voting classifier having based model as knn , naive bayes and Random Forest . this is trained on a huge data of spam and ham sms and have the accuracy of 87(approx). 
<br>
thanks for reading
